namespace PerformanceApp.Server.Auth.Constants;

public static class AuthConstants
{
    private const string _invalidLoginMessage = "Invalid username or password";
    public static string InvalidLoginMessage => _invalidLoginMessage;
}